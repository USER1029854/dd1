# -*- coding: utf-8 -*-
"""Per-incident Phase C specifics: (affected_functions, broken_invariant, decisive_missing_guard).
Authored from the normalized incident description plus, where retrieved, independent
technical sources (see sources/incident-references/corroboration_log.json)."""
S = {
"INC-2026-08-20-MAN": (["Cosmos EVM module (component not named publicly)"],"State written during EVM execution must be committed or discarded atomically with that execution, and a precompile must not leave partial writes behind when the call it belongs to does not complete.","Wrap each precompile execution in an atomic function that reverts partially committed state on error; supply-conservation invariant enforced per block."),
"INC-2026-08-22-TCH": (["Cosmos-based EVM side of the network (component not named publicly)"],"Total supply of the native asset must change only through the sanctioned minting path, and must reconcile across native, staked, bridged and wrapped representations.","Per-block total-supply conservation with alerting on any single-transaction delta outside the expected minting path."),
"INC-2026-08-18-MAY": (["Trade Account accounting","outbound handling","pool math","add/removeLiquidity"],"Subsidy and pool accounting must reconcile across Trade Account, outbound and liquidity modules; no module may credit value another module did not debit.","One canonical accounting path with a closing global invariant assert across modules."),
"INC-2026-08-15-FOX": (["FoxLpBondsPool.stake()","Treasury.lpBonds()","addLiquidity"],"Bond size must be derived from value actually contributed, not from an AMM spot quote captured before a state-changing swap in the same transaction.","Re-read or measure the realised contribution after the swap; TWAP/deviation bound on the quote."),
"INC-2026-08-10-USM": (["defund()","ethFromDefund()","adjShrinkFactor"],"Redeeming amount N once must yield no less than redeeming N/k k times; per-redemption price averaging and state contraction must be split-invariant.","Split-invariance assertion on the redemption price function."),
"INC-2026-08-09-COR": (["bridge deposit verification","relayer authorisation","multisig withdrawal"],"A deposit is only real when an external asset was escrowed; a self-transfer of the bridge's own wrapped token with a valid memo is not a deposit.","Deposit validation that rejects the bridge's own wrapped asset and requires genuine external escrow."),
"INC-2026-08-08-ATO": (["manager signature verification","LP position burn"],"A manager signature must authorise exactly one Uniswap V3 position; the signed digest must contain the position ID and a consumed nonce.","Position ID + nonce inside the EIP-712 digest, consumed once."),
"INC-2026-08-06-UNI": (["LaunchpadFactoryAuto (calldata injection path)","setApprovalForAll"],"A factory custodying users' LP NFTs must never execute caller-supplied calldata that can grant approvals over that custody.","Target/selector allowlist; no arbitrary calldata execution while holding custody."),
"INC-2026-08-02-LOO": (["LpdFi position open","interest claim at daily settlement","Cake-LP burn"],"Interest-bearing position size must not be set from a manipulable thin-pair spot price, and interest must be prorated by time held rather than claimable across a settlement boundary.","TWAP/deviation bound on the LPD/USDC price plus time-weighted interest accrual."),
"INC-2026-08-02-MOK": (["MokeToken.releaseContract()","claim()"],"A public claim must pay only a caller-specific entitlement that is checked and consumed once.","Eligibility mapping checked and consumed before transfer."),
"INC-2026-07-30-SET": (["ExchangeIssuance","manager pre-issue hook","positionMultiplier"],"Position valuation must be finalised before any external hook can observe or influence it; the state lock must span the hook window.","State lock covering the pre-issue hook, plus a closing invariant assert on positionMultiplier."),
"INC-2026-07-30-SWA": (["ZhaiquanBuy (_signer constant)","claim()/transfer signature path"],"Nothing in deployed bytecode is secret; authorisation must not depend on knowledge of an in-code signing key.","Signer private key held off-chain; only its address stored on-chain."),
"INC-2026-07-28-CRY": (["Pro token vault functions"],"Vault functions that move value must require a role an ordinary caller cannot obtain.","Role check on every value-moving external selector."),
"INC-2026-07-28-LUL": (["recycle()","PancakeSwap V2 reserve path"],"Token logic must not move or destroy tokens held by the AMM pair outside the pair's own accounting.","Pair excluded from all balance-mutating token logic."),
"INC-2026-07-25-PRO": (["trackPurchase()","massWithdraw()","skim()"],"Reward credit must be derived from economic value actually spent with the protocol, not from an observed token-balance delta on a third-party pair.","Credit sized from the protocol's own post-transfer balance delta."),
"INC-2026-07-24-LIE": (["BondMakerCollateralizedEth.exchangeEquivalentBonds()"],"Exchanging one bond set for another must conserve total backing; the multiset of inputs and outputs must be proven equivalent.","Value-conservation assert across the bond exchange."),
"INC-2026-07-23-VER": (["bridge import path","checkCCEValues"],"A destination payout must equal the value committed on the source chain; signature and proof validity do not imply value equality.","Assert payout amount == source-committed amount inside the same proof."),
"INC-2026-07-23-SOL": (["SOLID collateral oracle assignment","stale-feed fallback"],"A collateral price must come from a feed proven to describe that asset; a stale feed must revert rather than fall back to another asset's oracle.","Feed-to-asset binding asserted on registration; staleness check that reverts."),
"INC-2026-07-22-42D": (["Median Oracle","BTCB vault liquidation"],"Liquidation eligibility must derive from a price the liquidator cannot move.","Deviation-bounded independent feed plus a liquidation grace period."),
"INC-2026-07-19-ALL": (["Solana USDC/USDT pool swap curve"],"A round trip through the pool must cost at least the fees at every reachable ratio, including extreme imbalance.","Imbalance-scaled fees and per-block ratio-change caps."),
"INC-2026-07-17-ACR": (["Solana deposit event emission","relayer payout"],"A relayer payout must be bound to a deposit event that only the bridge program can emit.","Deposit proof bound to the bridge program's own state, not a spoofable event."),
"INC-2026-07-15-BAR": (["governance proposal execution","proxy upgrade","pre-existing user approvals"],"Governance passage and execution must be separated by a delay long enough to respond, and users' allowances must not be spendable by future, unknown implementation code.","Timelock plus guardian veto on upgrades reachable from governance."),
"INC-2026-07-15-CAS": (["CLS vault mark price","short liquidation"],"Mark price must not be movable by a party holding the opposing position.","Deviation-bounded mark price plus liquidation size caps."),
"INC-2026-07-14-DRI": (["DaiDripsHub.give()","uint128->int128 cast"],"A transfer amount must be non-negative; casts between signed and unsigned types must be range-checked.","SafeCast on every cross-sign cast."),
"INC-2026-07-13-LUM": (["Sodium smart account UserOp validation","Paymaster allowance"],"Account validation must not grant a spendable allowance as a side effect.","No allowance side effects during the validation phase; per-flow allowances only."),
"INC-2026-07-13-CHI": (["ArbitrageV5.burn()","hardcoded $1 peg"],"If minting validates the market peg, redemption must apply the symmetric check rather than redeeming at a hardcoded price.","Symmetric peg validation on mint and burn."),
"INC-2026-07-11-BON": (["Supra SAUCE price feed","borrow path"],"Borrow capacity must not exceed the cost of moving the price feed it is derived from.","Deviation bound against an independent source plus borrow caps sized to venue depth."),
"INC-2026-07-06-LAZ": (["totalAssets()","Silo Ark (impaired, not offboarded)","redeem()"],"totalAssets() must equal value the vault could realise now; an impaired component must not remain counted at book value, and a direct transfer into a counted component must not raise the share price.","Offboarding removes the component from totalAssets atomically with pausing it; per-block share-price change cap."),
"INC-2026-07-06-BON": (["BIP-76 governance proposal","treasury transfer"],"The cost of acquiring decisive voting power must exceed the value governance controls.","Timelock plus quorum scaled to controlled value."),
"INC-2026-07-02-HIN": (["deposit path (proofless)"],"Every value-bearing path into a shielded pool must require proof verification with no bypass branch.","Proof required on every deposit/withdraw path; nullifier consumed before release."),
"INC-2026-07-01-EDE": (["wGOOGLx exchange rate","deposit/borrow loop"],"Collateral value must not be derived from a wrapper exchange rate that the borrower can inflate through the protocol's own deposit/borrow loop.","Growth-capped rate adapter plus an independent market cross-check."),
"INC-2026-06-28-AID": (["_sellTransfer()","burn fee accounting"],"A transfer must conserve value: any fee burned must be debited from the sender's balance.","Conservation property test across every transfer branch."),
"INC-2026-06-25-LIX": (["lv_* wrapper EIP-2612 permit","withdrawFrom/withdrawETHFrom"],"Permit verification must reject a signature that does not recover to the token holder.","OZ ECDSA.recover, reverting on invalid signatures."),
"INC-2026-06-23-ROY": (["Royal1155LD.beforeLdaTransfer","pro-rata royalty accrual"],"A zero-value ERC-1155 transfer must be economically inert and must not drive royalty accrual.","require(amount > 0) before any hook side effect."),
"INC-2026-06-21-QUI": (["qATOM/qOSMO mint","proof verification"],"Minting a liquid-staking receipt must require a verified proof of the underlying delegation.","Proof verification enforced on every mint path."),
"INC-2026-06-19-JBX": (["JB sell cycle burn from pair","pair.sync()"],"Token logic must not burn tokens held by the AMM pair and immediately resync reserves, re-anchoring the curve after each extraction.","Pair excluded from burn logic; no public sync-triggering path."),
"INC-2026-06-20-LAB": (["OLPC _update()","decimalsValue parameter","pair burn + reserve desync"],"A configurable token parameter must be bounded, and token logic must never burn tokens held by the AMM pair.","Parameter bounds at the setter plus exclusion of the pair from burn logic."),
"INC-2026-06-17-AZT": (["deprecated Private Rollup Bridge escape hatch","rollup proof check"],"A deprecated deployment must retain no capability; an immutable escape hatch must still verify ownership and proofs.","Deprecation that drains value and disables every release path."),
"INC-2026-06-17-LIT": (["LBPHashrate._update()","_harvest","mintReward(pair)"],"A zero-value transferFrom must not trigger a privileged harvest, and minting must never inflate the pair's balance without a reserve update.","require(amount > 0) before the hook; never mint directly to a pair."),
"INC-2026-06-17-DIP": (["_transfer() (router branch, missing return)","skim()/sync()"],"Every branch of a transfer override must conserve value and execute exactly one transfer.","Conservation property test across all transfer branches."),
"INC-2026-06-15-THE": (["legacy vault mint/claim redemption math"],"Redemption math must remain correct as supply approaches zero; the zero/near-zero-supply branch must be guarded.","Virtual shares offset plus a minimum-supply floor."),
"INC-2026-06-14-AZT": (["deprecated Aztec Connect Router","proof verification"],"A deprecated router must not release funds, and proof verification must be complete.","Deprecation that drains value; complete public-input binding."),
"INC-2026-06-10-RAY": (["deprecated AMM V3 program","LP mint address validation"],"An LP mint supplied by the caller must be the one the program derived for that pool.","PDA/mint derivation asserted against the pool."),
"INC-2026-05-11-TAC": (["TAC sequencer set (inbound bridge message validation)","TON jetton wallet code hash","jetton master / minter field in wallet data","TAC Proxy mint path"],"A deposit may only be credited on the destination side if the sending account is proven to be the CANONICAL account for the claimed asset -- on TON, the jetton wallet whose code hash matches the standard jetton-wallet code and whose data points at the expected jetton master. A well-formed message from an arbitrary contract is a claim, not a deposit.","Verify the sender jetton wallet's code hash against the canonical jetton-wallet code and its minter field against the expected jetton master on every inbound message; reject unverifiable senders. Per-minter rate limits and a supply invariant as secondary defences."),
"INC-2026-06-10-SEC": (["modified CW20-ICS20 (ics20-for-axelar)","source channel check (removed)"],"Minting a wrapped asset must require the transfer to have arrived over the registered source channel and denomination path.","Source chain + channel + denom path asserted before mint."),
"INC-2026-06-09-AST": (["DN404/BT404 packed ownership","token ID alias"],"Packed token-ID aliases must be range-checked and arithmetic over packed fields must not underflow.","Checked arithmetic plus explicit token-ID range validation."),
"INC-2026-06-09-HAE": (["deprecated deposit path","new redeem path","LP share mint"],"Every path that mints or redeems vault shares must apply identical accounting; a deprecated path must be disabled atomically with the upgrade.","One canonical share-accounting function; legacy paths disabled on upgrade."),
"INC-2026-06-09-NOV": (["dividend distribution before balance update","deposit()"],"Dividend accrual must be checkpointed before any balance change that prices it.","Checkpoint-before-mutate ordering on every balance-changing path."),
"INC-2026-06-09-TOK": (["Aragon DAO proposal (no timelock)","TOP mint","Balancer V1 pool"],"Governance passage and execution must be separated by a delay, and the cost of control must exceed the value controlled.","Timelock plus quorum scaled to controlled value."),
"INC-2026-06-08-FLO": (["BT404 packed ownership","fpToken/$BMP mint"],"Packed ownership encoding must range-check token IDs and use checked arithmetic.","Checked arithmetic plus token-ID range validation."),
"INC-2026-06-08-AMB": (["HotProxy/WarmPath/ColdPath","surplus collateral accounting"],"Surplus-collateral accounting must be identical across every proxy path that can reach it.","Single canonical accounting path with a closing invariant assert."),
"INC-2026-06-05-DTX": (["DTXT transfer classification heuristic","sell fee bypass"],"Transfer-intent classification must derive from evidence the caller cannot fabricate.","Explicit protocol-recorded intent rather than balance inference."),
"INC-2026-06-04-ATM": (["transferFrom() (auto-swap ~20%)"],"A transfer must not perform value-moving side effects using protocol-held funds.","No external swaps inside transfer; fees taken by reducing the credited amount."),
"INC-2026-06-04-BYT": (["triggerAutoBurn() (public)","pair.sync()"],"A public maintenance function must not burn tokens held by the AMM pair and rewrite its reserves.","Pair excluded from burn logic; no public sync-triggering path."),
"INC-2026-06-03-APE": (["ApeYieldVault.migrateToVotingEscrow(poolIds[])"],"A caller-supplied pool-ID array must be deduplicated or each ID consumed inside the loop.","Mark each ID consumed inside the loop, or require strictly increasing IDs."),
"INC-2026-06-01-ATO": (["getReward()","Solady ReentrancyGuard fixed slot","rewards mapping"],"A re-entrancy sentinel written to a fixed storage slot must not be readable as a reward balance.","ERC-7201 namespaced storage plus storage-layout diffing."),
"INC-2026-05-29-MON": (["LegendaryMoneyMonNft.cliamRewred()","verify()/recoverSigner","admin == address(0)"],"Signature verification must reject a zero-address recovery, and address(0) must never be valid authority.","require(recovered != address(0)) and a non-zero admin assertion."),
"INC-2026-05-28-JOE": (["_removeLiquidityViaContract","lpInfo[user].lpAmount"],"State must be written before the external BNB transfer that can re-enter.","Checks-Effects-Interactions plus a global re-entrancy lock."),
"INC-2026-05-28-ONT": (["onlyOwner (accepts owner == address(0))","hidden balance-grant logic"],"A renounced owner must permanently remove capability; address(0) must never satisfy an authority check.","Assert owner != address(0) inside the modifier."),
"INC-2026-05-26-SKP": (["SKP token LP balance path","sync()"],"Token logic must not remove tokens from the pair's balance and rewrite reserves.","Pair excluded from balance-mutating logic."),
"INC-2026-05-25-WUS": (["WUSD._englove reward path","wrap/unwrap cycle"],"A per-address incentive must cost the claimant more than it pays, or bind to a non-duplicable identity.","Rewards proportional to time-weighted stake with lifetime per-address caps."),
"INC-2026-05-25-THI": (["SquidRouterModule (constant-string auth)","arbitrary calldata execution"],"A constant present in deployed code cannot authenticate anything, and a Safe module must not execute caller-chosen calldata.","Signature over a fresh nonce plus a target/selector allowlist."),
"INC-2026-05-23-MUR": (["MureDistribution proxy","SignatureChecker with caller-supplied signer source"],"Signature verification must resolve the signer from immutable protocol state, never from a caller-supplied address.","Signer fixed at deployment or set only through a timelock."),
"INC-2026-05-22-FRA": (["USDF vault deposit (0xb6b55f25)/withdraw","Balancer V2 batchSwap callback","fixed daily tokenPrice"],"A vault's share accounting must hold across re-entrant external callbacks, and a daily-fixed price must not be exploitable within its epoch.","Global re-entrancy lock plus a closing share/asset invariant assert."),
"INC-2026-05-20-BUT": (["OmniServiceProxy retry message hash (abi.encodePacked)"],"A message digest must be injective: no two distinct messages may hash equal.","abi.encode (not encodePacked) over dynamic fields."),
"INC-2026-05-19-HER": (["withdrawal verification script","key reset defence"],"Every withdrawal path must enforce zk verification; auxiliary scripts are part of the trust boundary.","Proof verification enforced on all release paths."),
"INC-2026-05-18-VER": (["bridge import path","checkCCEValues"],"A destination payout must equal the value committed on the source chain.","Assert payout amount == source-committed amount inside the same proof."),
"INC-2026-05-13-SHA": (["Colony Network meta-transaction self-call","DSAuth"],"A contract must not be able to forward a call that authorises itself.","address(this) explicitly rejected as an authorised caller."),
"INC-2026-05-12-AUR": (["SafeOwnable Facet initialize(address)","_initialized slot == 0","diamondCut"],"An initializer must be provably consumed after deployment; ownership must not be re-establishable.","initializer modifier with the flag asserted non-zero post-deploy."),
"INC-2026-05-12-SQP": (["Staking contract hardcoded owner backdoor","EIP-7702 authorizationList takeover"],"Authorisation must not depend on a value present in verified source, and no hidden owner path may exist.","No in-code authorisation constants; ownership through a timelocked, published path."),
"INC-2026-05-11-INK": (["Workspace Treasury Proxy claim()","whitelisted claimer identity"],"A whitelist must bind to an immutable address, not to a property an attacker-deployed contract can reproduce.","Registry membership checked by address."),
"INC-2026-05-10-REN": (["Dark Pool proxy unprotected initializer","version counter out of sync","delegatecall"],"An initializer must not be reachable on a live deployment, and a migration must leave version state consistent.","Post-deploy assertion that re-initialization reverts."),
"INC-2026-05-05-EKU": (["v2 extension IPayer.pay callback","transferFrom(payer, Core, amount)"],"A callback that pulls funds must verify both that its caller is the core contract and that the payer initiated the current lock.","require(msg.sender == core) plus payer bound to the lock initiator."),
"INC-2026-04-29-AFT": (["perps fee accounting","negative builder-code fee"],"A fee parameter must be clamped to a non-negative bounded range at the setter.","Fee parameters clamped to [0, max] at the setter."),
"INC-2026-04-28-YIE": (["YieldCore-3rd-deal vault withdrawal (missing caller check)"],"A vault withdrawal must require the caller to hold the position being withdrawn.","Caller authorisation on every value-moving selector."),
"INC-2026-04-28-JUD": (["JUDAO sell-burn path","BUSD-JUDAO pair reserves"],"Token logic must not burn from the pair and rewrite reserves.","Pair excluded from burn logic."),
"INC-2026-04-27-SIN": (["Uniswap V3 oracle fee tier 42","factory.getPool() == address(0)","VaultTokensLib.totalAssets()"],"An oracle must revert when its pool lookup fails rather than returning a zero price, and totalAssets() must not silently omit valued reserves.","Zero-address and zero-price checks that revert; fee-tier validation at registration."),
"INC-2026-04-26-SCA": (["deprecated V2 rewards contract","update_points (uninitialized variable)"],"A reward accumulator must be initialised to the user's join-time index before any accrual.","User index initialised on first interaction; deprecated pools drained."),
"INC-2026-04-23-GID": (["GiddyVaultV3 EIP-712 SwapInfo (partial coverage)"],"Every economically decisive field (aggregator, fromToken, toToken, amount) must be inside the signed digest.","Typehash covering the full parameter set, with nonce and deadline."),
"INC-2026-04-22-KIP": (["Kipseli Router","USDC-only quoter output used as tokenOut amount"],"A quoted amount must be re-bound to the output asset and its decimals before transfer.","Assert quoteToken == outputToken and normalise decimals; use measured balance deltas."),
"INC-2026-04-20-THE": (["new vault deposit (totalAssets == 0, totalSupply == 0)"],"The first depositor must not be able to set an arbitrary asset-per-share ratio.","Virtual shares offset or dead shares minted at creation."),
"INC-2026-04-20-JUI": (["REVLoans.borrowFrom","caller-supplied REVLoanSource (.terminal,.token)"],"The accounting context of a loan must be resolved from a protocol registry, never supplied by the caller.","Terminal/token resolved from the registry; oracle never skipped."),
"INC-2026-04-19-CUS": (["public b2a13230() arbitrary target.call(data)","Aave V3 credit delegation"],"A contract holding credit delegation must never execute caller-specified calls.","Target/selector allowlist; delegation revoked when idle."),
"INC-2026-04-16-RHE": (["margin engine multi-step swap route","aggregate slippage check","attacker-created Ref Finance pools"],"Slippage protection must be enforced per hop against real venue economics; a caller-supplied route may not be trusted in aggregate.","Per-hop minimum-out enforcement plus a routable-venue allowlist."),
"INC-2026-04-15-LOO": (["xLoot redeem(tokenIds[])","_redeemable()","nextRedeem"],"Duplicate token IDs must not accumulate; the redeem marker must be written before payout.","Consume each ID inside the loop."),
"INC-2026-04-13-HYP": (["token gateway forged message","admin permission grant"],"A cross-chain message must not be able to grant administrative authority without a proof bound to a real source event.","Message binding to source chain, nonce and action, with authority changes timelocked."),
"INC-2026-04-13-DAN": (["insurance fund donate (no positive-amount check)"],"A donation amount must be verified positive before it adjusts fund accounting.","require(amount > 0) with explicit bounds."),
"INC-2026-04-13-MON": (["_handleSell()","burnsellMona()","BurnAddress.burn()","sync()"],"Payout and burn must be atomic; a deferred burn must not later remove tokens from the pair and rewrite reserves.","Atomic settlement; pair excluded from deferred burn."),
"INC-2026-04-12-SUB": (["Settings.setContractAddress() (onlyOwner removed in refactor)"],"A setter that rebinds StakingManager/RewardsDistributor must require an owner role.","Role check on every config setter, asserted at deploy time."),
"INC-2026-04-07-SQU": (["SquidMulticall.run() (permissionless, arbitrary calldata)"],"A permissionless executor must not spend allowances granted to it by third parties.","Value moved only from msg.sender; no standing approvals to executors."),
"INC-2026-04-07-TGA": (["TGAI reserve path","sync() with USDT injection"],"Pair reserves must change only through the pair's own accounting.","No public sync-triggering path after a protocol-initiated balance change."),
"INC-2026-04-04-BSC": (["TMM burn to dead address","pair reserve reduced to 1 TMM"],"Token logic must not burn tokens held by the pair.","Pair excluded from burn logic."),
"INC-2026-04-03-EIP": (["EIP-7702 delegated code pancakeV3SwapCallback() (no access control)"],"A swap callback on a delegated account must verify that its caller is the expected pool.","require(msg.sender == expectedPool) in every callback."),
"INC-2026-04-02-SAS": (["SAS sellBurn counter","ordinary transfer burning from pool","sync()"],"A deferred burn credit must not be redeemable against the pair's balance by an unrelated transfer.","Pair excluded from burn logic; no deferred burn state."),
"INC-2026-03-31-LML": (["staking reward calc using stored price (3600s cooldown)","EIP-7702 batch claim"],"Rewards must be sized from the same price at which they can be realised; a stored price with a long cooldown must not size claims against a live market.","Reward sizing from live TWAP with a deviation bound, or claim delay matching the cooldown."),
"INC-2026-03-26-UNK": (["Stake contract reward calc from TUR-NOBEL spot","referral claim"],"Reward size must not derive from a spot price the claimant can move.","TWAP plus deviation bound on the reward price input."),
"INC-2026-03-24-MOO": (["Moonriver governance proposal","comptroller/oracle/market admin transfer"],"The cost of decisive voting power must exceed controlled value, and execution must be delayed enough to respond.","Timelock plus guardian veto; quorum scaled to controlled value."),
"INC-2026-03-23-BCE": (["BCE burn mechanism","buy/sell restriction bypass","pair reserves"],"Token logic must not burn from the pair, and restriction logic must not be bypassable by a helper contract.","Pair excluded from burn logic; no address-based exemptions for callable contracts."),
"INC-2026-03-22-CYR": (["CyrusTreasury.withdrawUSDTFromAny()","PancakeSwap V3 ETH/USDT spot"],"LP withdrawal size must not be derived from a spot price the caller can move in the same transaction.","TWAP plus deviation bound on the withdrawal pricing."),
"INC-2026-03-17-DTR": (["dLEND dUSD market first deposit"],"The first depositor must not be able to set an arbitrary asset-per-share ratio.","Virtual shares offset or factory-seeded markets."),
"INC-2026-03-15-VEN": (["vTHE getCashPrior()","exchangeRateStored","supply cap"],"An exchange rate must be a function of accounted deposits; a direct token transfer must not raise it, and the supply cap must bind the rate as well as the mint.","Internally tracked cash counter plus per-block rate-change caps."),
"INC-2026-03-14-GOO": (["StrategyGooseEgg deposit()/withdraw() before reward settlement"],"Share issuance must be checkpointed against settled rewards, not against pending ones.","Checkpoint-before-mutate ordering; same-block deposit-and-claim disallowed."),
"INC-2026-03-12-AMU": (["AM toBurnAmount deferred burn","pair.sync()"],"A deferred fee must not be burned from the pair's balance and the reserves resynced.","Pair excluded from burn logic."),
"INC-2026-03-12-STA": (["L1BlockOracleUpdater","LaPoste cross-chain bridge","vote proof verification"],"L1 block data injected cross-chain must be bound to a proven source-chain state, not accepted from a peripheral updater.","Source-bound proof for injected block data; updater behind a timelock."),
"INC-2026-03-12-AAV": (["CAPO (Capped Asset Price Oracle) wstETH parameters"],"A price cap adapter must never report a value materially below the asset's true value; liquidation must not proceed on a capped-low price.","Deviation bound against the uncapped feed plus a liquidation grace period."),
"INC-2026-03-11-DBX": (["ERC-2771 sender resolution"],"_msgSender() must resolve to the same principal wherever authorisation is evaluated.","Single immutable trusted forwarder; multicall disabled on ERC-2771 paths."),
"INC-2026-03-10-MTW": (["pendingBurnAmount","distributeFees() burning from pair","router/pair whitelist bypass","0.2 MT referral exemption"],"Token logic must not burn from the pair, and transfer restrictions must not be bypassable through whitelisted routers or referral exemptions.","Pair excluded from burn logic; no exemptions for callable contracts."),
"INC-2026-03-09-GON": (["Sell & Repay Purchase Bundler ownership check bypass"],"Escrowed NFTs may be released only to the proven owner of the position.","Ownership check on every release path."),
"INC-2026-03-07-MOL": (["onlySpawnerToken modifier","mintFromSpawner()"],"A minter relationship must bind to a registered address, not to a shape an attacker-deployed contract can satisfy.","Registry membership checked by address."),
"INC-2026-03-02-CUR": (["CryptoFromPoolVaultWAgg oracle","sDOLA.convertToAssets()","DolaSavings.stake()","LLAMMA soft liquidation"],"A vault share rate is not a market price; collateral valuation must not be atomically inflatable by a donation or redemption cycle.","Growth-capped rate adapter with a hard deviation bound plus a liquidation grace period."),
"INC-2026-02-26-FOO": (["zkSNARK verifying key (misconfigured)"],"A verifying key must be immutable and hash-matched to the published circuit artifact.","Immutable, attested verifying key."),
"INC-2026-02-26-PLO": (["price oracle mapping (BTC/USD feed assigned to USDC)"],"A feed must be proven to describe the asset it prices.","Feed-to-asset binding asserted on registration (base/quote validated)."),
"INC-2026-02-22-BLE": (["YieldBlox pool USTRY collateral config","Reflector oracle","SDEX price source"],"Borrow capacity must never exceed the cost of moving the price of the collateral that unlocks it on its deepest venue.","Borrow caps sized to measured venue depth plus a deviation bound."),
}
